  <header style="background-color: #212529;">
    <%= link_to image_tag("logo.png", class: "logo", alt: "logo", style: "width: 100px; height: 100px;"), root_path %>
    </header>
<div class="container">
  <div class="card text-center m-5">
    <div class="card-body">
      <h2>Log in</h2>

      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "Log in", class: "btn btn-primary" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>

<footer class="text-white text-center fixed-bottom bg-dark">
  <p>© <%= Time.now.year %> Middle</p>
</footer>
